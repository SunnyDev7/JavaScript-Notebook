const User = ["hitesh", 3, "admin"]

var [name, courseCount, role] = User



var Myuser = {
    name: "x",
    courseCount : 5,
    role: "admin"
}


const {name, courseCount, role} = Myuser
console.log(role)